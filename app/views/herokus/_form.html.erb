<%= form_for(@heroku) do |f| %>
  <% if @heroku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heroku.errors.count, "error") %> prohibited this heroku from being saved:</h2>

      <ul>
      <% @heroku.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :year %><br />
    <%= f.number_field :year %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
